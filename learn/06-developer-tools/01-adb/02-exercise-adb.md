# Exercise — ADB Practice
> **Time:** 30 min
## 📝 Tasks
1. List devices, install WagwanWorld via CLI, take a screenshot, pull it to your PC.
2. Use `adb shell am start` to launch your app without Android Studio.
3. Use `adb logcat -s` to filter logs by your app's tag.
4. Use `adb shell dumpsys meminfo` to check your app's memory usage.
5. Use `adb reverse` to connect your app to a local development server.
---
⬅️ [ADB Commands](01-adb-commands.md) | ➡️ [Studio Profiler](../02-android-studio/01-profiler.md)
